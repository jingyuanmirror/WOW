# 皮肤列表页 PRD

## 文档信息
- **版本**: v1.0
- **创建日期**: 2025-12-28
- **最后更新**: 2025-12-28
- **适用产品**: WOW UI Skin Designer 品牌官网
- **页面**: 皮肤列表页 (Skin Gallery/Portfolio)

---

## 1. 页面概述

### 1.1 页面定位
皮肤列表页是展示所有可用皮肤作品的核心页面，承担着作品集展示、用户浏览、内容发现的关键职责。作为纯展示型页面，不涉及实际售卖功能，专注于提供优质的浏览体验和作品预览能力。

### 1.2 页面目标
- **作品展示**: 系统化展示所有皮肤作品，让用户全面了解设计师的作品系列
- **高效筛选**: 提供多维度筛选和排序，帮助用户快速找到感兴趣的皮肤
- **沉浸体验**: 通过图片/视频预览，让用户直观感受皮肤效果
- **用户引导**: 引导用户浏览详情、收藏喜欢的作品、了解更多信息
- **品牌传播**: 通过作品集整体呈现，强化品牌专业度和设计能力

### 1.3 核心指标 (KPI)
- **页面停留时间** > 2分钟
- **每次访问浏览皮肤数** > 5个
- **筛选/排序使用率** > 40%
- **预览弹窗打开率** > 60%
- **皮肤详情转化率** > 30%
- **喜欢/收藏率** > 15%
- **页面加载时间** < 1.5秒

---

## 2. 页面结构与功能模块

### 2.1 页面布局

#### 2.1.1 整体架构
```
┌─────────────────────────────────────────────────────────────┐
│  导航栏 (全局)                                                │
├─────────────────────────────────────────────────────────────┤
│  页面标题区 (Page Header)                                     │
│  - 标题: "皮肤作品集"                                         │
│  - 副标题/描述                                                │
│  - 统计信息 (总计XX款皮肤)                                    │
├─────────────────────────────────────────────────────────────┤
│  筛选与排序工具栏 (Filter & Sort Bar)                        │
│  ┌─────────────────┬──────────────┬──────────────────────┐  │
│  │ 版本筛选         │ 排序方式      │ 视图切换 + 搜索       │  │
│  └─────────────────┴──────────────┴──────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  主内容区 (Main Content)                                     │
│  ┌──────────┬──────────┬──────────┬──────────┐             │
│  │  皮肤卡片 │  皮肤卡片 │  皮肤卡片 │  皮肤卡片 │             │
│  ├──────────┼──────────┼──────────┼──────────┤             │
│  │  皮肤卡片 │  皮肤卡片 │  皮肤卡片 │  皮肤卡片 │             │
│  └──────────┴──────────┴──────────┴──────────┘             │
├─────────────────────────────────────────────────────────────┤
│  分页控制区 (Pagination)                                     │
└─────────────────────────────────────────────────────────────┘
```

#### 2.1.2 响应式适配
- **桌面端 (≥1280px)**: 4列网格布局
- **平板端 (768px-1279px)**: 3列网格布局
- **移动端 (<768px)**: 2列网格布局，筛选工具栏收起为抽屉

---

### 2.2 页面头部区 (Page Header)

#### 2.2.1 功能描述
提供页面标题、简要说明和整体统计信息，帮助用户快速了解当前页面内容。

#### 2.2.2 内容元素
```
皮肤作品集
探索专为魔兽世界打造的精美UI皮肤，提升你的游戏体验

总计 48 款皮肤 | 支持正式服、60版、80版
```

#### 2.2.3 设计要点
- 标题采用大号字体，视觉突出
- 副标题使用柔和色彩，提供简短说明
- 统计信息动态更新，显示当前筛选结果数量
- 背景可选用渐变或魔兽主题纹理

---

### 2.3 筛选与排序工具栏 (Filter & Sort Bar)

#### 2.3.1 功能描述
提供多维度筛选和排序能力，帮助用户快速定位目标皮肤。工具栏固定在内容区上方，滚动时可选择性吸顶。

#### 2.3.2 筛选维度

**版本筛选 (Version Filter)**
- **位置**: 工具栏左侧
- **交互**: 多选按钮组或下拉多选框
- **选项**:
  - 全部版本 (默认)
  - 正式服 (Retail)
  - 60版 (Classic)
  - 80版 (Wrath)
- **逻辑**: 支持多选，选中任一版本则显示支持该版本的皮肤（OR关系）
- **状态显示**: 选中项高亮，显示选中数量徽章

**标签筛选 (Tag Filter)** - 可选扩展
- **位置**: 版本筛选右侧
- **交互**: 下拉多选框或抽屉式标签墙
- **选项**:
  - PvP专用
  - PvE副本
  - 治疗界面
  - DPS输出
  - 简洁风格
  - 华丽风格
  - 暗色主题
  - 亮色主题
- **逻辑**: 多选，AND关系（需同时满足所有选中标签）

**品质筛选 (Quality Filter)** - 可选扩展
- **选项**: 普通/稀有/史诗/传说
- **UI**: 使用对应品质颜色的复选框或色块

#### 2.3.3 排序方式 (Sort Options)

**位置**: 工具栏中部
**交互**: 下拉选择器
**选项**:
- **默认排序** (推荐): 综合考虑更新时间、热度、品质
- **最新优先**: 按最后更新时间倒序
- **喜欢最多**: 按喜欢数量倒序 (默认)
- **名称 A-Z**: 按皮肤名称字母顺序
- **名称 Z-A**: 按皮肤名称字母倒序

**当前排序显示**:
```
排序: 喜欢最多 ▼
```

#### 2.3.4 辅助功能区

**位置**: 工具栏右侧
**包含元素**:

1. **视图切换器**:
   - 网格视图 (默认): 卡片式，显示图片+基本信息
   - 列表视图: 紧凑型，显示更多详细信息

2. **搜索框** (可选):
   - 占位符: "搜索皮肤名称..."
   - 实时搜索，输入时动态过滤列表
   - 支持中英文、拼音首字母

3. **筛选重置按钮**:
   - 显示条件: 有任何筛选/排序被激活时显示
   - 文案: "清除筛选"
   - 点击后恢复默认状态

#### 2.3.5 筛选状态提示

当有筛选条件激活时，工具栏下方显示筛选标签：
```
已选择: [正式服 ×] [60版 ×] [PvP专用 ×]  清除全部
```

---

### 2.4 皮肤卡片 (Skin Card)

#### 2.4.1 功能描述
作为列表的核心单元，每张卡片展示单个皮肤的关键信息和预览入口。卡片设计需要平衡信息密度和视觉美感。

#### 2.4.2 卡片结构 (网格视图)

```
┌─────────────────────────────────┐
│  ┌───────────────────────────┐  │
│  │                           │  │  ← 预览图区域 (16:9 比例)
│  │      预览图/视频缩略图      │  │     悬停时播放视频或展示动效
│  │                           │  │
│  └───────────────────────────┘  │
│  ┌─────────────────────────────┐│
│  │ 🎬 [图片] [视频]            ││  ← 媒体类型指示器
│  └─────────────────────────────┘│
│  皮肤名称                        │  ← 标题 (最多2行，超出省略)
│  by 设计师名称                   │  ← 作者信息 (灰色小字)
│  ──────────────────────────────│
│  ❤️ 1,234  👁️ 5,678  💬 89     │  ← 互动数据
│  ──────────────────────────────│
│  [正式服] [60版] [PvP]          │  ← 版本标签+分类标签
│  ──────────────────────────────│
│  [查看详情] [喜欢 ❤️]             │  ← 操作按钮
└─────────────────────────────────┘
```

#### 2.4.3 卡片元素详解

**1. 预览图区域**
- **尺寸**: 建议16:9比例，适配游戏界面截图
- **默认显示**: 主预览图 (静态)
- **悬停效果**:
  - 如有视频: 自动播放循环视频（静音）
  - 如仅图片: 显示图片轮播指示器，支持鼠标悬停切换
  - 显示半透明遮罩 + "点击预览"提示
- **点击行为**: 打开预览弹窗 (Modal)
- **加载处理**: 显示骨架屏或模糊占位符

**2. 媒体类型指示器**
- **位置**: 预览图底部覆盖层或下方独立栏
- **图标**:
  - 🖼️ 图片 (X张): 显示图片数量
  - 🎬 视频 (X分Y秒): 显示视频时长
- **样式**: 半透明深色背景 + 白色图标和文字

**3. 皮肤名称**
- **字号**: 16-18px，加粗
- **颜色**: 根据品质等级显示对应色彩
  - 普通: #FFFFFF (白色)
  - 稀有: #0070DD (蓝色)
  - 史诗: #A335EE (紫色)
  - 传说: #FF8000 (橙色)
- **长度限制**: 最多显示2行，超出显示省略号
- **悬停**: 显示完整名称的tooltip

**4. 作者信息**
- **格式**: "by 设计师名称"
- **样式**: 小字号 (12px)，次要文字色
- **点击**: 跳转到设计师主页 (如有)

**5. 互动数据栏**
- **喜欢数**: ❤️ 数字 (支持k/w简写，如1.2k)
- **浏览数**: 👁️ 数字
- **评论数**: 💬 数字 (可选)
- **更新日期**: 🕐 X天前 (可选，显示最后更新)
- **样式**: 小图标 + 数字，横向排列，间距均匀

**6. 标签区**
- **版本标签**: 
  - 样式: 圆角胶囊形状，蓝色底色
  - 文案: "正式服" "60版" "80版"
  - 逻辑: 显示所有支持的版本
- **分类标签**:
  - 样式: 圆角胶囊形状，灰色底色
  - 文案: "PvP" "PvE" "治疗" "DPS" 等
  - 逻辑: 最多显示3个标签，超出显示"+N"
- **品质色条** (可选):
  - 位置: 卡片顶部或左侧边框
  - 宽度: 2-4px
  - 颜色: 根据品质等级

**7. 操作按钮区**
- **查看详情按钮**:
  - 样式: 主要按钮 (Primary)
  - 文案: "查看详情"
  - 行为: 跳转到皮肤详情页 `/skins/[id]`
- **喜欢按钮**:
  - 样式: 次要按钮 (Secondary) 或图标按钮
  - 图标: ❤️ (未喜欢) / ❤️ (已喜欢，红色填充)
  - 行为: 切换喜欢状态，更新喜欢数
  - 未登录: 提示登录或保存到本地

#### 2.4.4 卡片状态

**常规状态**:
- 背景: 深色卡片背景
- 边框: 细边框或无边框
- 阴影: 轻微阴影

**悬停状态**:
- 轻微上浮动画 (translateY: -4px)
- 阴影加深
- 预览图播放视频或显示切换提示

**点击状态**:
- 轻微缩放动画

**加载状态**:
- 骨架屏占位
- 图片懒加载

#### 2.4.5 列表视图 (List View)

当用户切换到列表视图时，卡片结构调整为横向布局：

```
┌──────────────────────────────────────────────────────────────┐
│ [预览图]  皮肤名称                    ❤️ 1,234  [正式服] [60版]│
│  120x90   简短描述文字...             👁️ 5,678  [PvP]        │
│           by 设计师 | 更新于2天前      💬 89     [查看详情] ❤️ │
└──────────────────────────────────────────────────────────────┘
```

- 预览图更小 (120x90px)
- 信息更密集，一行展示更多内容
- 适合快速浏览大量皮肤

---

### 2.5 预览弹窗 (Preview Modal)

#### 2.5.1 功能描述
点击卡片预览图或"点击预览"提示时，打开全屏或大尺寸弹窗，提供沉浸式的图片/视频预览体验。

#### 2.5.2 弹窗结构

```
┌─────────────────────────────────────────────────────────────┐
│  ✕                                                           │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                                                     │    │
│  │               主图片/视频播放区                      │    │
│  │                  (最大化显示)                        │    │
│  │                                                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ◀  [缩略图1] [缩略图2] [缩略图3] [缩略图4]  ▶                │
│                                                              │
│  皮肤名称 - by 设计师                                         │
│  ❤️ 1,234 喜欢  |  [查看完整详情]  [喜欢 ❤️]                   │
└─────────────────────────────────────────────────────────────┘
```

#### 2.5.3 弹窗功能

**1. 主显示区**
- **图片模式**:
  - 高清大图居中显示
  - 支持鼠标滚轮/手势缩放
  - 支持拖拽查看不同区域
  - 双击放大/还原
- **视频模式**:
  - 视频播放器，支持播放/暂停、进度条、音量控制
  - 全屏按钮
  - 循环播放开关

**2. 缩略图轮播**
- 显示所有可用的图片和视频缩略图
- 点击切换主显示内容
- 当前显示项高亮边框
- 支持左右箭头导航
- 超过5个时支持横向滚动

**3. 信息与操作栏**
- 皮肤名称和作者
- 喜欢数
- "查看完整详情"按钮: 跳转到详情页
- "喜欢"按钮: 切换喜欢状态

**4. 键盘导航**
- `Esc`: 关闭弹窗
- `←/→`: 切换上一张/下一张
- `Space`: 播放/暂停视频

**5. 关闭方式**
- 点击右上角 ✕ 按钮
- 点击弹窗外部区域
- 按 `Esc` 键

#### 2.5.4 移动端适配
- 弹窗全屏显示
- 支持左右滑动切换图片
- 双指缩放
- 底部操作栏固定

---

### 2.6 空状态与加载状态

#### 2.6.1 加载状态
**初次加载**:
- 显示12个卡片骨架屏
- 骨架屏包含预览图占位、文字占位
- 使用脉冲动画或渐变动画

**筛选加载**:
- 主内容区显示半透明遮罩
- 中央显示Loading指示器
- 或采用乐观UI，立即更新筛选条件，后台刷新数据

#### 2.6.2 空状态
**无筛选结果**:
```
🔍

未找到符合条件的皮肤

尝试调整筛选条件或清除所有筛选

[清除筛选]
```

**无皮肤数据**:
```
📦

暂无皮肤作品

敬请期待更多精彩作品

[返回首页]
```

---

### 2.7 分页控制 (Pagination)

#### 2.7.1 分页策略
**推荐方案**: 无限滚动 + 可选分页器

- **无限滚动**:
  - 滚动到底部自动加载下一页
  - 显示"加载更多..."指示器
  - 适合移动端和沉浸式浏览
  - 每页加载16-24个卡片

- **传统分页器** (可选):
  - 位置: 底部中央
  - 显示当前页码和总页数
  - 支持快速跳转到指定页
  - 适合精确定位

**URL同步**:
- 筛选条件和页码同步到URL参数
- 例: `/skins?version=retail,classic&sort=likes&page=2`
- 支持浏览器前进/后退
- 支持分享和书签

#### 2.7.2 分页器UI
```
[首页]  [上一页]  1  2  [3]  4  5  ...  10  [下一页]  [末页]

共 240 款皮肤，当前第 3 页，共 10 页
```

---

## 3. 数据模型

### 3.1 皮肤数据结构 (Skin Object)

```typescript
interface Skin {
  id: string;                          // 唯一标识符
  name: string;                        // 皮肤名称
  nameEn?: string;                     // 英文名称 (可选)
  description: string;                 // 简短描述 (1-2句话)
  descriptionFull?: string;            // 完整描述 (详情页用)
  
  // 作者信息
  author: {
    id: string;
    name: string;
    avatar?: string;
  };
  
  // 品质等级
  quality: 'common' | 'rare' | 'epic' | 'legendary';
  
  // 版本支持
  versions: Array<'retail' | 'classic' | 'wrath'>;
  
  // 分类标签
  tags: string[];                      // 如 ['PvP', '治疗', '简洁风格']
  
  // 媒体资源
  media: {
    thumbnail: string;                 // 缩略图 URL (用于卡片)
    images: Array<{
      url: string;
      width: number;
      height: number;
      alt?: string;
    }>;
    videos?: Array<{
      url: string;
      thumbnail: string;
      duration: number;                // 秒
    }>;
  };
  
  // 互动数据
  stats: {
    likes: number;                     // 喜欢数
    views: number;                     // 浏览数
    downloads?: number;                // 下载数 (可选)
    comments?: number;                 // 评论数 (可选)
  };
  
  // 时间信息
  createdAt: string;                   // ISO 8601 格式
  updatedAt: string;                   // ISO 8601 格式
  
  // 排序权重 (后台管理)
  sortOrder?: number;                  // 数字越大越靠前
  
  // 其他
  featured?: boolean;                  // 是否精选 (首页展示)
  status: 'draft' | 'published' | 'archived';  // 状态
}
```

### 3.2 筛选与排序参数

```typescript
interface SkinFilterParams {
  // 版本筛选
  versions?: Array<'retail' | 'classic' | 'wrath'>;
  
  // 标签筛选
  tags?: string[];
  
  // 品质筛选
  quality?: Array<'common' | 'rare' | 'epic' | 'legendary'>;
  
  // 搜索关键词
  search?: string;
  
  // 排序方式
  sort?: 'default' | 'latest' | 'likes' | 'name-asc' | 'name-desc';
  
  // 分页
  page?: number;
  pageSize?: number;
}
```

---

## 4. 交互与用户体验

### 4.1 交互流程

**主流程 1: 浏览皮肤**
1. 用户进入皮肤列表页
2. 页面加载，显示默认排序的皮肤（喜欢最多）
3. 用户滚动浏览卡片
4. 悬停卡片查看视频预览
5. 点击"喜欢"收藏感兴趣的皮肤
6. 点击"查看详情"进入详情页

**主流程 2: 筛选皮肤**
1. 用户点击版本筛选，选择"正式服"
2. 列表实时更新，仅显示支持正式服的皮肤
3. 用户再选择"PvP"标签
4. 列表进一步过滤，显示符合条件的皮肤
5. 用户调整排序为"最新优先"
6. 列表重新排序

**主流程 3: 预览皮肤**
1. 用户点击卡片预览图
2. 弹窗打开，显示第一张高清图片
3. 用户点击视频缩略图
4. 主显示区切换为视频播放
5. 用户点击"查看完整详情"
6. 跳转到皮肤详情页

### 4.2 性能优化

**加载优化**:
- 首屏SSR渲染前12个卡片
- 预览图使用Next.js Image组件优化
- 懒加载屏幕外卡片
- 视频预览按需加载

**交互优化**:
- 筛选/排序使用乐观UI，立即更新界面
- 喜欢操作本地先更新，后台同步
- 无限滚动预加载下一页

**缓存策略**:
- SWR缓存皮肤列表数据
- 筛选结果缓存30秒
- 已浏览皮肤缓存到本地

### 4.3 无障碍 (A11Y)

- 所有图片提供alt文本
- 筛选控件支持键盘操作
- Focus状态明显可见
- 使用语义化HTML标签
- ARIA标签辅助屏幕阅读器
- 色彩对比度符合WCAG AA标准

### 4.4 移动端适配

**触控优化**:
- 按钮和点击区域足够大 (最小44x44px)
- 支持左右滑动切换预览图片
- 支持下拉刷新

**布局调整**:
- 筛选工具栏收起为抽屉/底部弹出菜单
- 2列网格布局
- 操作按钮堆叠排列

---

## 5. 技术实现建议

### 5.1 技术栈

**前端框架**:
- Next.js 16 App Router
- React 19 Server Components

**UI组件**:
- shadcn/ui: Button, Card, Badge, Select, Checkbox, Dialog, Skeleton
- Radix UI: 底层组件库
- Lucide Icons: 图标库

**状态管理**:
- SWR: 数据获取、缓存、重新验证
- Zustand: 全局筛选状态、喜欢状态 (可选本地持久化)
- URL状态: 筛选参数同步

**样式**:
- Tailwind CSS v4
- CSS变量管理主题色

**媒体处理**:
- `next/image`: 图片优化
- `embla-carousel-react`: 缩略图轮播
- 自定义视频播放器组件

**动画**:
- Framer Motion: 卡片动画、弹窗动画
- CSS Transitions: 简单交互动画

### 5.2 组件结构

```
src/app/skins/
├── page.tsx                    // 列表页主入口 (SSR)
├── loading.tsx                 // Loading UI
└── error.tsx                   // Error UI

src/components/skins/
├── skin-list-header.tsx        // 页面头部
├── skin-filter-bar.tsx         // 筛选与排序工具栏
│   ├── version-filter.tsx      // 版本筛选
│   ├── tag-filter.tsx          // 标签筛选
│   ├── sort-selector.tsx       // 排序选择器
│   └── view-switcher.tsx       // 视图切换
├── skin-grid.tsx               // 皮肤网格容器
├── skin-card.tsx               // 皮肤卡片 (客户端组件)
├── skin-list-item.tsx          // 列表视图项
├── skin-preview-modal.tsx      // 预览弹窗 (已存在，复用)
├── skin-pagination.tsx         // 分页控制
└── skin-empty-state.tsx        // 空状态

src/lib/
├── hooks/
│   ├── use-skins.ts            // SWR钩子，获取皮肤列表
│   ├── use-skin-filters.ts     // 筛选状态管理
│   └── use-like-skin.ts        // 喜欢操作
└── utils/
    └── skin-filters.ts         // 筛选逻辑工具函数
```

### 5.3 数据获取

**服务端组件 (SSR)**:
```typescript
// src/app/skins/page.tsx
export default async function SkinsPage({
  searchParams,
}: {
  searchParams: { [key: string]: string | string[] | undefined };
}) {
  const filters = parseSearchParams(searchParams);
  const initialData = await fetchSkins(filters);

  return (
    <div>
      <SkinListHeader total={initialData.total} />
      <SkinFilterBar initialFilters={filters} />
      <SkinGrid initialData={initialData} filters={filters} />
    </div>
  );
}
```

**客户端组件 (CSR)**:
```typescript
// src/components/skins/skin-grid.tsx
'use client';

export function SkinGrid({ initialData, filters }) {
  const { data, error, isLoading } = useSkins(filters, {
    fallbackData: initialData,
  });

  if (isLoading) return <SkinGridSkeleton />;
  if (error) return <ErrorState />;
  if (!data?.skins.length) return <EmptyStat />;

  return (
    <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
      {data.skins.map((skin) => (
        <SkinCard key={skin.id} skin={skin} />
      ))}
    </div>
  );
}
```

### 5.4 筛选实现

**URL状态同步**:
```typescript
// src/lib/hooks/use-skin-filters.ts
export function useSkinFilters() {
  const router = useRouter();
  const searchParams = useSearchParams();

  const updateFilter = (key: string, value: any) => {
    const params = new URLSearchParams(searchParams);
    
    if (value) {
      params.set(key, Array.isArray(value) ? value.join(',') : value);
    } else {
      params.delete(key);
    }

    router.push(`/skins?${params.toString()}`, { scroll: false });
  };

  return { filters: parseSearchParams(searchParams), updateFilter };
}
```

### 5.5 喜欢功能

**本地状态 + 后端同步**:
```typescript
// src/lib/hooks/use-like-skin.ts
export function useLikeSkin(skinId: string) {
  const { data: user } = useUser();
  const { mutate } = useSWRConfig();
  
  const [isLiked, setIsLiked] = useState(false);

  const toggleLike = async () => {
    // 乐观更新
    setIsLiked(!isLiked);
    
    // 更新缓存
    mutate(`/api/skins/${skinId}`, (data) => ({
      ...data,
      stats: {
        ...data.stats,
        likes: data.stats.likes + (isLiked ? -1 : 1),
      },
    }), false);

    // 后端同步
    try {
      await fetch(`/api/skins/${skinId}/like`, {
        method: isLiked ? 'DELETE' : 'POST',
      });
    } catch (error) {
      // 失败时回滚
      setIsLiked(isLiked);
      mutate(`/api/skins/${skinId}`);
    }
  };

  return { isLiked, toggleLike };
}
```

---

## 6. API接口设计 (参考)

### 6.1 获取皮肤列表

**接口**: `GET /api/skins`

**查询参数**:
```typescript
{
  versions?: string;          // 逗号分隔，如 "retail,classic"
  tags?: string;              // 逗号分隔
  quality?: string;           // 逗号分隔
  search?: string;
  sort?: string;              // "default" | "latest" | "likes" | "name-asc" | "name-desc"
  page?: number;
  pageSize?: number;          // 默认24
}
```

**响应**:
```json
{
  "skins": [
    {
      "id": "skin-001",
      "name": "暗影突袭",
      "description": "专为PvP设计的简洁高效界面",
      "quality": "epic",
      "versions": ["retail", "wrath"],
      "tags": ["PvP", "简洁风格"],
      "media": {
        "thumbnail": "/images/skins/skin-001/thumb.webp",
        "images": [
          { "url": "/images/skins/skin-001/1.webp", "width": 1920, "height": 1080 }
        ],
        "videos": [
          { "url": "/videos/skin-001/preview.mp4", "thumbnail": "...", "duration": 30 }
        ]
      },
      "stats": {
        "likes": 1234,
        "views": 5678,
        "downloads": 890
      },
      "author": {
        "id": "author-001",
        "name": "UI大师"
      },
      "createdAt": "2024-11-15T10:00:00Z",
      "updatedAt": "2024-12-20T15:30:00Z"
    }
  ],
  "total": 48,
  "page": 1,
  "pageSize": 24,
  "totalPages": 2
}
```

### 6.2 喜欢/取消喜欢

**接口**: `POST /api/skins/:id/like` (喜欢)  
**接口**: `DELETE /api/skins/:id/like` (取消喜欢)

**响应**:
```json
{
  "success": true,
  "likes": 1235
}
```

---

## 7. 设计规范

### 7.1 色彩系统

**品质色彩**:
- 普通: `#FFFFFF` (白色)
- 稀有: `#0070DD` (蓝色)
- 史诗: `#A335EE` (紫色)
- 传说: `#FF8000` (橙色)

**版本标签**:
- 正式服: `#3B82F6` (蓝色)
- 60版: `#10B981` (绿色)
- 80版: `#8B5CF6` (紫色)

**主题色**:
- 主色: `#FFD700` (金色)
- 背景: `#0F172A` (深蓝黑)
- 卡片背景: `#1E293B`
- 文字主色: `#F1F5F9`
- 文字次要: `#94A3B8`

### 7.2 间距与尺寸

**网格间距**: 24px (桌面) / 16px (移动端)
**卡片圆角**: 12px
**按钮圆角**: 8px
**标签圆角**: 16px (胶囊形)

**卡片尺寸**:
- 4列布局: 每列 ~300px
- 3列布局: 每列 ~350px
- 2列布局: 每列 ~400px

### 7.3 动画规范

**时长**:
- 悬停效果: 200ms
- 页面切换: 300ms
- 弹窗打开/关闭: 250ms

**缓动函数**:
- 默认: `cubic-bezier(0.4, 0.0, 0.2, 1)`
- 弹性: `cubic-bezier(0.68, -0.55, 0.265, 1.55)`

---

## 8. 测试与验收

### 8.1 功能测试

**筛选功能**:
- ✅ 版本筛选正确过滤皮肤
- ✅ 多个版本选择为OR关系
- ✅ 标签筛选正确过滤皮肤
- ✅ 多个筛选条件可同时生效
- ✅ 清除筛选恢复默认状态

**排序功能**:
- ✅ 默认排序展示推荐皮肤
- ✅ 喜欢最多正确排序
- ✅ 最新优先按时间倒序
- ✅ 名称排序正确

**预览功能**:
- ✅ 点击卡片预览图打开弹窗
- ✅ 弹窗显示高清图片
- ✅ 视频可正常播放
- ✅ 缩略图切换主显示内容
- ✅ 键盘导航可用

**喜欢功能**:
- ✅ 点击喜欢按钮切换状态
- ✅ 喜欢数实时更新
- ✅ 已喜欢状态正确显示

**分页功能**:
- ✅ 无限滚动自动加载下一页
- ✅ 传统分页器正确跳转
- ✅ URL参数同步

### 8.2 性能测试

- ✅ 首屏加载时间 < 1.5秒
- ✅ 图片懒加载生效
- ✅ 视频按需加载
- ✅ 筛选/排序响应时间 < 300ms
- ✅ 无限滚动流畅，无明显卡顿

### 8.3 兼容性测试

**浏览器**:
- ✅ Chrome (最新版本)
- ✅ Firefox (最新版本)
- ✅ Safari (最新版本)
- ✅ Edge (最新版本)

**设备**:
- ✅ 桌面端 (1920x1080, 1366x768)
- ✅ 平板端 (iPad, 1024x768)
- ✅ 移动端 (iPhone, Android)

### 8.4 无障碍测试

- ✅ 键盘可完整操作所有功能
- ✅ 屏幕阅读器可正确朗读内容
- ✅ 色彩对比度符合WCAG AA标准
- ✅ Focus状态明显可见

---

## 9. 后续迭代计划

### 9.1 Phase 2 功能
- 高级筛选: 支持创建时间范围筛选
- 保存筛选条件: 用户可保存常用筛选组合
- 比较功能: 选择多个皮肤并排对比
- 个性化推荐: 基于浏览历史推荐相似皮肤

### 9.2 Phase 3 功能
- 用户评分系统: 允许用户对皮肤评分
- 评论系统: 每个皮肤支持评论
- 分享功能: 分享皮肤到社交媒体
- 收藏夹: 用户创建自己的皮肤收藏夹

### 9.3 优化方向
- A/B测试不同排序默认值
- 优化推荐算法
- 增加更多视频预览
- 提升移动端体验

---

## 10. 附录

### 10.1 参考链接
- [Next.js Image Optimization](https://nextjs.org/docs/app/building-your-application/optimizing/images)
- [shadcn/ui Components](https://ui.shadcn.com/docs/components)
- [SWR Documentation](https://swr.vercel.app/)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)

### 10.2 设计资源
- Figma设计稿: [链接待补充]
- 品牌色彩规范: [链接待补充]
- 组件库Storybook: [链接待补充]

---

**文档版本历史**:
- v1.0 (2025-12-28): 初始版本，定义核心功能与交互
